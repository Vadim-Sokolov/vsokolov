SELECT c.name, p.name FROM person as p inner join company as c on p.company_id = c.id where c.id <> 5;

select c.name, count(*) as total_people from person as p inner join company as c on p.company_id = c.id group by c.name order by total_people desc limit 1;